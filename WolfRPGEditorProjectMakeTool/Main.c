#include <stdio.h>
int main(vold) {
  printf("=======================================\nWolfRPGエディタープロジェクト作成ツール\n=======================================\n");
  
  FILE *file;
 
  if (file = fopen("WolfRPGEditorProjectMakeTool.ini", "r"))
    {
      fclose(file);
      printf("OK.\n");
    }
  else
    {
      printf("Not Found.\n");
      return 1;
    }
  if (file =) setopne
  return 0;
}
